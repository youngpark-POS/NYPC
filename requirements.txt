# NYPC AlphaZero Project Requirements
# 이 파일은 전체 프로젝트에서 필요한 Python 패키지를 정의합니다.
# 
# 설치 방법:
#   pip install -r requirements.txt
#
# 또는 conda 환경에서:
#   conda create -n nypc python=3.11
#   conda activate nypc
#   pip install -r requirements.txt

# 핵심 머신러닝 패키지
# PyTorch - 딥러닝 프레임워크 (CUDA 지원 버전)
# CUDA 버전 설치: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# CPU 버전 설치: pip install torch torchvision torchaudio
torch>=2.0.0
torchvision>=0.15.0        # PyTorch 컴퓨터 비전 라이브러리
torchaudio>=2.0.0          # PyTorch 오디오 처리 라이브러리
numpy>=1.20.0              # NumPy - 수치 연산

# C++ 바인딩
pybind11>=2.10.0          # C++ Python 바인딩 (fast_game_board용)

# 개발 및 빌드 도구
setuptools>=45.0          # Python 패키징
wheel>=0.35.0             # 바이너리 패키지 생성
cmake>=3.12.0             # 빌드 시스템 (pip 버전, 시스템 설치 권장)

# 데이터 저장 및 처리
h5py>=3.7.0              # HDF5 파일 포맷 (게임 히스토리 저장용)

# 테스트 도구
pytest>=6.0.0            # 단위 테스트
pytest-benchmark         # 성능 벤치마크

# 참고: fast_game_board.pyd는 C++ 확장으로 별도 빌드 필요
# 빌드 방법: cd practice/alphazero/cpp && build.bat